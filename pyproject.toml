[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "kbeauty-analysis"
version = "0.1.0"
description = "K-beauty Amazon market and TikTok virality analysis"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
